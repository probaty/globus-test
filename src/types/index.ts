export interface Employee {
  id: string;
  organizationId: string;
  firstName: string;
  lastName: string;
  position: string;
}
export interface Organization {
  id: string;
  name: string;
  address: string;
}
